<?php
$host = "localhost";
$username = "root";
$password = "";
$database = "id22036239_teepay";

$db = mysqli_connect($host, $username, $password, $database);

if (!$db) {
    die("Connection failed: " . mysqli_connect_error());
} else {
    echo"";
}
?>
